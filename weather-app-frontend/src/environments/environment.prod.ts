// src/environments/environment.prod.ts

export const environment = {
    production: true,
    apiUrl: 'https://api.miproduccion.com' // Ejemplo de una variable de entorno para la API URL en producción
    // Agrega otras variables de entorno según sea necesario para tu proyecto de producción
  };
  